# Wagtail Unboxed — pinned for the whole series so every episode is reproducible.
# Wagtail 7.4 declares only `Django>=5.2` (no upper bound), so an unpinned install
# drifts. We pin both. See docs/prerequisites.md.
wagtail==7.4.3
Django==6.1.1

# Production (episode 13)
whitenoise==6.12.0
dj-database-url==3.1.2
psycopg[binary]==3.3.6
